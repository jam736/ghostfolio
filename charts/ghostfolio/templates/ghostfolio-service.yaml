apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -c -f docker-compose.yml
    kompose.version: 1.32.0 (765fde254)
  labels:
    io.kompose.service: ghostfolio
  name: ghostfolio
spec:
  ports:
    - name: "3333"
      port: " {{ .Values.ghostfolioport }}"
      targetPort: " {{ .Values.ghostfolioport }}"
  selector:
    io.kompose.service: ghostfolio
